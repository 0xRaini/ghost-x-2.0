# Ghost X 2.0 本地预览指南

## 快速开始

### 方法一：Chrome 浏览器本地加载（推荐）

这是预览和测试 Chrome 扩展的标准方法：

1. **打开 Chrome 扩展管理页面**
   - 在 Chrome 地址栏输入：`chrome://extensions/`
   - 或者：菜单 → 更多工具 → 扩展程序

2. **启用开发者模式**
   - 点击页面右上角的"开发者模式"开关

3. **加载扩展**
   - 点击左上角"加载已解压的扩展程序"按钮
   - 选择项目根目录：`/Users/rain/Desktop/AI Coding 项目/Ghost X 2.0`
   - 点击"选择"

4. **验证加载成功**
   - 扩展列表中应该出现 "Ghost X"
   - 图标显示为蓝色幽灵
   - 状态应为"已启用"

5. **开始使用**
   - 访问 Twitter/X 网站：
     - https://twitter.com
     - https://x.com
   - 点击浏览器工具栏的 Ghost X 图标打开弹窗
   - 开始使用幽灵功能

---

## 本地预览地址

### Chrome 扩展页面
```
chrome://extensions/
```
在这里可以查看扩展状态、重新加载、查看错误

### 测试页面
```
https://twitter.com
https://x.com
```
Ghost X 只在这两个域名下工作

### 扩展弹窗
点击工具栏图标 → 弹出 popup.html

### 开发者工具
- 右键点击扩展图标 → "审查弹出内容" → 查看 popup 调试信息
- 在 Twitter 页面按 F12 → 查看 content script 控制台输出

---

## 实时开发工作流

### 1. 编辑代码
在你喜欢的编辑器中修改文件：
- [content.js](content.js) - 核心功能逻辑
- [popup.js](popup.js) - 弹窗界面逻辑
- [popup.html](popup.html) - 弹窗界面结构
- [manifest.json](manifest.json) - 扩展配置

### 2. 重新加载扩展

**重要**：修改代码后必须重新加载！

#### 方法 A：扩展页面重新加载
1. 前往 `chrome://extensions/`
2. 找到 Ghost X 扩展
3. 点击刷新图标 🔄

#### 方法 B：使用快捷键
1. 在扩展页面直接按 `Ctrl+R` (Windows/Linux) 或 `Cmd+R` (Mac)

### 3. 刷新测试页面
- 在 Twitter/X 页面按 `F5` 或 `Cmd+R`
- content.js 的修改需要刷新网页才能生效

### 4. 查看调试信息
```javascript
// content.js 的日志在网页控制台
console.log('GhostX 已加载设置:', currentSettings);

// popup.js 的日志在弹窗控制台
console.log('弹窗已加载');
```

---

## 常见问题排查

### 问题 1: 扩展加载失败

**症状**：点击"加载已解压的扩展程序"后出现错误

**解决方案**：
```bash
# 1. 检查 manifest.json 语法
cd "/Users/rain/Desktop/AI Coding 项目/Ghost X 2.0"
cat manifest.json | python3 -m json.tool

# 2. 检查图标文件是否存在
ls -la images/icon*.png

# 3. 查看详细错误信息
# 在 chrome://extensions/ 页面点击"错误"按钮
```

### 问题 2: 功能不工作

**症状**：扩展加载了但功能没反应

**解决方案**：
1. 打开 Twitter/X 页面
2. 按 `F12` 打开开发者工具
3. 查看 Console 标签是否有错误
4. 确认是否看到 "GhostX 已加载设置" 日志

### 问题 3: 修改代码不生效

**症状**：改了代码但看不到效果

**解决方案**：
1. 前往 `chrome://extensions/`
2. 找到 Ghost X，点击刷新 🔄
3. 回到 Twitter 页面，按 `Cmd+Shift+R` 强制刷新
4. 清除缓存：`Cmd+Shift+Delete`

### 问题 4: 存储数据丢失

**症状**：重新加载扩展后数据消失

**解决方案**：
```bash
# 查看存储的数据
# 1. 前往 chrome://extensions/
# 2. 点击 Ghost X 的"检查视图"
# 3. 在控制台输入：
chrome.storage.local.get(null, console.log)
chrome.storage.sync.get(null, console.log)
```

---

## 调试技巧

### 1. Content Script 调试

在 Twitter/X 页面打开控制台：

```javascript
// 查看当前设置
chrome.storage.sync.get(['ghostModeEnabled', 'filterKeyword'], console.log)

// 查看所有幽灵回复
chrome.storage.local.get(null, (data) => {
    const replies = Object.keys(data).filter(k => k.startsWith('ghost-reply-'))
    console.log('所有回复：', replies)
})

// 手动触发推文处理
processAllTweets()
```

### 2. Popup 调试

右键点击扩展图标 → "审查弹出内容"：

```javascript
// 查看统计数据
loadReplyStats()
loadRetweetStats()
loadLikeStats()

// 查看群组信息
chrome.storage.local.get(['currentGroup'], console.log)
```

### 3. 性能监控

```javascript
// 在 content.js 添加性能标记
console.time('processTweets')
processAllTweets()
console.timeEnd('processTweets')
```

### 4. 网络请求监控

打开开发者工具 → Network 标签：
- 查看是否有失败的请求
- 检查 Twitter API 调用

---

## 推荐的开发设置

### VS Code 配置

创建 `.vscode/settings.json`：

```json
{
  "files.associations": {
    "*.js": "javascript"
  },
  "editor.formatOnSave": true,
  "editor.tabSize": 4
}
```

### Chrome 开发者工具设置

1. **启用持久日志**
   - Console → 齿轮图标 → "Preserve log"
   - 页面刷新后不清除日志

2. **禁用缓存**
   - Network → "Disable cache"
   - 开发时不使用缓存

3. **Source Maps**
   - Settings → "Enable JavaScript source maps"
   - 更好的调试体验

---

## 测试清单

在发布前测试这些功能：

### 基础功能
- [ ] 扩展可以成功加载
- [ ] 图标正确显示
- [ ] 弹窗可以打开

### 幽灵模式
- [ ] 开启/关闭幽灵模式
- [ ] 关键词过滤生效
- [ ] 刷新页面后设置保持

### 幽灵回复
- [ ] 可以添加回复
- [ ] 字符计数正确
- [ ] 回复显示在推文下方
- [ ] 可以删除回复
- [ ] 刷新后回复保留

### 幽灵转发/收藏
- [ ] 可以转发/收藏
- [ ] 统计数字更新
- [ ] 显示在推文下方

### 群组功能
- [ ] 可以创建群组
- [ ] 邀请码生成
- [ ] 可以加入群组
- [ ] 成员列表显示
- [ ] 可以离开群组

### 统计功能
- [ ] 统计数字正确
- [ ] 点击查看详情
- [ ] 清空功能工作

---

## 构建发布版本

当你准备发布时：

### 1. 清理开发文件
```bash
cd "/Users/rain/Desktop/AI Coding 项目/Ghost X 2.0"

# 删除不需要的文件
rm -rf archive/
rm -rf .DS_Store
rm 本地预览指南.md
```

### 2. 创建发布包
```bash
# 创建 zip 文件用于上传到 Chrome Web Store
zip -r ghost-x-2.0.zip . \
  -x "*.git*" \
  -x "*archive*" \
  -x "*node_modules*" \
  -x "*.DS_Store"
```

### 3. 版本号管理
编辑 [manifest.json](manifest.json:4)：
```json
{
  "version": "2.0.3"
}
```

遵循语义化版本号：
- 主版本号：重大功能变更
- 次版本号：新增功能
- 修订号：bug 修复

---

## 有用的链接

### 官方文档
- [Chrome 扩展开发文档](https://developer.chrome.com/docs/extensions/)
- [Manifest V3 迁移指南](https://developer.chrome.com/docs/extensions/mv3/intro/)
- [Chrome Storage API](https://developer.chrome.com/docs/extensions/reference/storage/)

### 开发工具
- [Chrome 扩展源代码查看器](https://chrome.google.com/webstore/detail/chrome-extension-source-v/jifpbeccnghkjeaalbbjmodiffmgedin)
- [Extension Reloader](https://chrome.google.com/webstore/detail/extensions-reloader/fimgfedafeadlieiabdeeaodndnlbhid) - 自动重新加载扩展

### Twitter/X 相关
- [Twitter Web Intent URLs](https://developer.twitter.com/en/docs/twitter-for-websites/web-intents/overview)
- [X/Twitter DOM 结构变化追踪](https://github.com/topics/twitter-dom)

---

## 下一步

完成预览测试后，你可以：

1. **添加更多功能**
   - 查看 [README.md](README.md) 的"未来计划"部分

2. **性能优化**
   - 减少 DOM 操作
   - 添加节流/防抖
   - 优化存储查询

3. **发布扩展**
   - 注册 Chrome Web Store 开发者账号
   - 准备商店列表素材
   - 提交审核

4. **收集反馈**
   - 邀请用户测试
   - 创建 GitHub Issues
   - 迭代改进

---

**祝你开发愉快！** 🚀

如有问题，请查看 [README.md](README.md) 或提交 Issue。
